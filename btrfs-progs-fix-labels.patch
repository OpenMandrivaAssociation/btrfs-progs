--- btrfs-progs-0.19/mkfs.c.labels~	2011-11-21 16:27:17.000000000 +0100
+++ btrfs-progs-0.19/mkfs.c	2011-12-01 15:29:06.118004966 +0100
@@ -339,7 +339,6 @@ static u64 parse_profile(char *s)
 
 static char *parse_label(char *input)
 {
-	int i;
 	int len = strlen(input);
 
 	if (len >= BTRFS_LABEL_SIZE) {
@@ -347,12 +346,6 @@ static char *parse_label(char *input)
 			BTRFS_LABEL_SIZE - 1);
 		exit(1);
 	}
-	for (i = 0; i < len; i++) {
-		if (input[i] == '/' || input[i] == '\\') {
-			fprintf(stderr, "invalid label %s\n", input);
-			exit(1);
-		}
-	}
 	return strdup(input);
 }
 
